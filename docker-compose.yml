version: '3'

services:
  github-to-text:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      # Mount the current directory to /app/output for easy access to generated files
      - ./output:/Users/nash/dev/projects/github_to_text
      # Optionally mount local repos if you want to process local repositories
      # - /path/to/local/repos:/repos
    # The default command shows the help message
    # Override with your own command when running
    command: --help